import React from "react";

const page = (metric: string) => {
  const hi = metric;
  return (
    <div className="flex flex-col items-center text-4xl pt-36 mb-10">
      THESE ARE THE METRICS *images from public folder go here*
    </div>
  );
};

export default page;
